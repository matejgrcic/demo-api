import * as express from 'express';

// eslint-disable-next-line import/prefer-default-export
export const registerUser = (_: express.Request, res: express.Response): void => {
    // validate
    res.sendStatus(200);
};
